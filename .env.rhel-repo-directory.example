# RHEL/CentOS Local YUM Repository Configuration (Directory Type)
# Copy this file to .env.rhel-repo-directory and configure your values

# ===================================================================
# Repository Type Configuration
# ===================================================================
# Type of repository: "directory"
# - Directory with RPM files (requires createrepo)
RHEL_REPO_TYPE=directory

# ===================================================================
# Directory Repository Configuration
# ===================================================================
# Source directory containing RPM files
RHEL_REPO_SOURCE_DIR=/root/repo

# Target directory where repository will be located
RHEL_REPO_TARGET_DIR=/usr/local/repo

# ===================================================================
# YUM Repository Configuration
# ===================================================================
# YUM repository configuration file path
RHEL_REPO_FILE=/etc/yum.repos.d/local-repo.repo

# Repository ID (used in .repo file)
RHEL_REPO_ID=local-repo

# Repository name (human-readable description)
RHEL_REPO_NAME="Local Repository"

# ===================================================================
# HTTP Server Configuration
# ===================================================================
# HTTP package name (httpd for RHEL/CentOS)
HTTPD_PACKAGE=httpd

# HTTP service name
HTTPD_SERVICE=httpd

# HTTP configuration
HTTPD_DOCUMENT_ROOT=/var/www/html
HTTPD_REPO_SYMLINK_NAME=rhel-repo
HTTPD_PORT=8080

# HTTP virtual host configuration file
HTTPD_VHOST_FILE=/etc/httpd/conf.d/rhel-repo.conf
