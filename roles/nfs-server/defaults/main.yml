---
# NFS Server Default Variables
nfs_server_enabled: false

# Default NFS exports configuration
nfs_exports:
  - path: "/nfs/shared"
    options: "rw,sync,no_subtree_check,no_root_squash"
    clients: "*"

# Directories to create for NFS exports
nfs_export_directories:
  - "/nfs/shared"