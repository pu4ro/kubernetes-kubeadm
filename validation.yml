---
# Kubernetes Cluster Validation Playbook
# Verifies the health of the cluster after installation
# Usage: ansible-playbook -i inventory.ini validation.yml

- name: Validate Kubernetes Cluster Health
  hosts: masters[0]
  become: yes
  roles:
    - { role: validate_cluster, tags: ['validation'] }
