# RHEL/CentOS Local YUM Repository Configuration (ISO Type)
# Copy this file to .env.rhel-repo-iso and configure your values

# ===================================================================
# Repository Type Configuration
# ===================================================================
# Type of repository: "iso"
# - ISO file mounted as repository (RHEL installation media)
RHEL_REPO_TYPE=iso

# ===================================================================
# ISO Repository Configuration
# ===================================================================
# Path to the RHEL ISO file
RHEL_REPO_ISO_PATH=/root/rhel-8.x-x86_64-dvd.iso

# Mount point for the ISO
RHEL_REPO_ISO_MOUNT_POINT=/mnt/rhel-iso

# ===================================================================
# YUM Repository Configuration
# ===================================================================
# YUM repository configuration file path
RHEL_REPO_FILE=/etc/yum.repos.d/local-repo.repo

# Repository ID (used in .repo file)
RHEL_REPO_ID=local-repo

# Repository name (human-readable description)
RHEL_REPO_NAME="Local Repository"

# ===================================================================
# HTTP Server Configuration
# ===================================================================
# HTTP package name (httpd for RHEL/CentOS)
HTTPD_PACKAGE=httpd

# HTTP service name
HTTPD_SERVICE=httpd

# HTTP configuration
HTTPD_DOCUMENT_ROOT=/var/www/html
HTTPD_REPO_SYMLINK_NAME=rhel-repo
HTTPD_PORT=8080

# HTTP virtual host configuration file
HTTPD_VHOST_FILE=/etc/httpd/conf.d/rhel-repo.conf
